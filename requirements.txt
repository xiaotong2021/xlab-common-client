# Python 依赖包
# 用于构建和 App Store Connect API 集成

# 图像处理（用于生成应用图标和启动画面）
Pillow>=10.0.0

# JWT 令牌生成（用于 App Store Connect API 认证）
PyJWT>=2.8.0

# HTTP 请求（用于 API 调用）
requests>=2.31.0

# 加密库（用于 JWT 签名）
cryptography>=41.0.0


