name: Build iOS App

# ──────────────────────────────────────────────────────────────────────────────
# 触发条件：推送 v* tag 时触发构建
# ──────────────────────────────────────────────────────────────────────────────
on:
  push:
    tags:
      - "v*"

# ──────────────────────────────────────────────────────────────────────────────
# 全局常量（与 Xcode project.pbxproj Release 配置保持一致）
# ──────────────────────────────────────────────────────────────────────────────
env:
  XCODE_VERSION:      "15.0.1"
  PROJECT:            "Hamster.xcodeproj"
  SCHEME:             "Hamster"
  HAMSTER_DIR:        "Hamster"
  BUNDLE_ID_MAIN:     "dev.fuxiao.app.Hamster"
  BUNDLE_ID_KEYBOARD: "dev.fuxiao.app.Hamster.HamsterKeyboard"

jobs:
  # ============================================================================
  # Job 1 ── 读取构建配置
  # ============================================================================
  prepare:
    name: Prepare Build Configuration
    runs-on: ubuntu-latest
    outputs:
      need_debug: ${{ steps.config.outputs.need_debug }}
      app_version: ${{ steps.config.outputs.app_version }}
      app_display_name: ${{ steps.config.outputs.app_display_name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Read build configuration
        id: config
        run: |
          APP_NAME=$(grep 'appName=' assets/build.app | cut -d'=' -f2)
          CONFIG_FILE="assets/$APP_NAME/app.cfg"

          NEED_DEBUG=$(grep '^needDebug=' $CONFIG_FILE | cut -d'=' -f2 | tr '[:upper:]' '[:lower:]')
          APP_VERSION=$(grep '^appVersion=' $CONFIG_FILE | cut -d'=' -f2)
          APP_DISPLAY_NAME=$(grep '^appDisplayName=' $CONFIG_FILE | cut -d'=' -f2)

          NEED_DEBUG=${NEED_DEBUG:-false}

          echo "need_debug=$NEED_DEBUG"              >> $GITHUB_OUTPUT
          echo "app_version=$APP_VERSION"            >> $GITHUB_OUTPUT
          echo "app_display_name=$APP_DISPLAY_NAME"  >> $GITHUB_OUTPUT

          echo "Need Debug: $NEED_DEBUG"
          echo "App Version: $APP_VERSION"
          echo "App Display Name: $APP_DISPLAY_NAME"

  # ============================================================================
  # Job 2 ── iOS 构建（Debug + Release）+ GitHub Release
  #
  #  所需 GitHub Secrets:
  #    KEYCHAIN_PASSWORD                        临时 keychain 密码（任意字符串）
  #    IOS_TEAM_ID                              Apple 开发者 Team ID
  #    IOS_CERTIFICATE_BASE64                   发布证书 .p12 的 base64 编码
  #    IOS_CERTIFICATE_PASSWORD                 .p12 证书的保护密码
  #    IOS_PROVISIONING_PROFILE_BASE64          主 App 描述文件 .mobileprovision 的 base64
  #    IOS_KEYBOARD_PROVISIONING_PROFILE_BASE64 键盘扩展描述文件 .mobileprovision 的 base64
  #    IOS_EXPORT_METHOD                        导出方式: app-store / ad-hoc / development
  #    TOKEN                                    GitHub Token（用于 Release）
  # ============================================================================
  build:
    name: Build Hamster iOS App
    needs: prepare
    runs-on: macos-13
    permissions:
      contents: write

    steps:
      # ── 拉取代码 ──────────────────────────────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          token: ${{ secrets.TOKEN }}

      # ── 切换 Xcode 版本 ────────────────────────────────────────────────────────
      - name: Select Xcode ${{ env.XCODE_VERSION }}
        run: sudo xcode-select -s "/Applications/Xcode_${{ env.XCODE_VERSION }}.app"

      # ── 校验 Frameworks ────────────────────────────────────────────────────────
      - name: Verify Frameworks
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          required=(
            "boost_atomic.xcframework"   "boost_filesystem.xcframework"
            "boost_locale.xcframework"   "boost_regex.xcframework"
            "boost_system.xcframework"   "icudata.xcframework"
            "icui18n.xcframework"        "icuio.xcframework"
            "icuuc.xcframework"          "libglog.xcframework"
            "libleveldb.xcframework"     "libmarisa.xcframework"
            "libopencc.xcframework"      "librime.xcframework"
            "librime-sbxlm.xcframework"  "libyaml-cpp.xcframework"
          )
          missing=()
          for fw in "${required[@]}"; do
            [ -d "Frameworks/$fw" ] && echo "  ✓ $fw" || { echo "  ✗ $fw ← MISSING"; missing+=("$fw"); }
          done
          [ ${#missing[@]} -eq 0 ] && echo "✅ 所有 xcframework 就绪" || { echo "❌ 缺失: ${missing[*]}"; exit 1; }

      # ── 编译输入方案（rime-ice 等）──────────────────────────────────────────────
      - name: Build Schemas
        working-directory: ${{ env.HAMSTER_DIR }}
        run: make schema

      # ── 清理 DerivedData ────────────────────────────────────────────────────────
      - name: Clean DerivedData
        run: rm -rf ~/Library/Developer/Xcode/DerivedData/*

      # ========== iOS Debug 构建（模拟器，无需签名） ==========
      - name: Build Debug (Simulator)
        if: needs.prepare.outputs.need_debug == 'true'
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          xcodebuild build \
            -project "${{ env.PROJECT }}" \
            -scheme  "${{ env.SCHEME }}" \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build-debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Package Debug .app
        if: needs.prepare.outputs.need_debug == 'true'
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          APP_PATH=$(find build-debug -name "*.app" -not -path "*/PlugIns/*" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "❌ 未找到 .app 编译产物"
            exit 1
          fi
          echo "📦 打包: $APP_PATH"
          zip -r Hamster-Debug-Simulator.zip "$APP_PATH"

      # ========== iOS Release 构建（真机签名） ==========
      - name: Install Certificate & Provisioning Profiles
        env:
          CERTIFICATE_BASE64:      ${{ secrets.IOS_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD:    ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          PROFILE_MAIN_BASE64:     ${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}
          PROFILE_KEYBOARD_BASE64: ${{ secrets.IOS_KEYBOARD_PROVISIONING_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD:       ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # ── 创建临时 keychain ───────────────────────────────────────────────
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain  -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

          # ── 导入 .p12 证书 ─────────────────────────────────────────────────
          echo "$CERTIFICATE_BASE64" | base64 --decode > certificate.p12
          security import certificate.p12 \
            -k build.keychain \
            -P "$CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security set-key-partition-list \
            -S apple-tool:,apple: \
            -s -k "$KEYCHAIN_PASSWORD" build.keychain

          # ── 安装主 App Provisioning Profile ────────────────────────────────
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles

          echo "$PROFILE_MAIN_BASE64" | base64 --decode > profile_main.mobileprovision
          MAIN_UUID=$(security cms -D -i profile_main.mobileprovision | plutil -extract UUID raw -)
          MAIN_NAME=$(security cms -D -i profile_main.mobileprovision | plutil -extract Name raw -)
          cp profile_main.mobileprovision \
            ~/Library/MobileDevice/Provisioning\ Profiles/${MAIN_UUID}.mobileprovision
          echo "主 App Profile: $MAIN_NAME (UUID: $MAIN_UUID)"

          # ── 安装键盘扩展 Provisioning Profile ──────────────────────────────
          echo "$PROFILE_KEYBOARD_BASE64" | base64 --decode > profile_keyboard.mobileprovision
          KEYBOARD_UUID=$(security cms -D -i profile_keyboard.mobileprovision | plutil -extract UUID raw -)
          KEYBOARD_NAME=$(security cms -D -i profile_keyboard.mobileprovision | plutil -extract Name raw -)
          cp profile_keyboard.mobileprovision \
            ~/Library/MobileDevice/Provisioning\ Profiles/${KEYBOARD_UUID}.mobileprovision
          echo "键盘扩展 Profile: $KEYBOARD_NAME (UUID: $KEYBOARD_UUID)"

          # ── 获取证书签名身份（SHA-1）────────────────────────────────────────
          CERT_IDENTITY=$(
            security find-identity -v -p codesigning build.keychain \
            | grep -E "Apple Distribution|iPhone Distribution" \
            | head -1 | awk '{print $2}'
          )
          if [ -z "$CERT_IDENTITY" ]; then
            echo "❌ 未找到有效的 Distribution 证书"
            security find-identity -v build.keychain
            exit 1
          fi
          echo "证书 Identity: $CERT_IDENTITY"

          # ── 写入 GITHUB_ENV ─────────────────────────────────────────────────
          echo "MAIN_PROFILE_NAME=$MAIN_NAME"         >> $GITHUB_ENV
          echo "KEYBOARD_PROFILE_NAME=$KEYBOARD_NAME" >> $GITHUB_ENV
          echo "CERT_IDENTITY=$CERT_IDENTITY"         >> $GITHUB_ENV

      # 归档阶段不签名，签名在 exportArchive 统一完成。
      # 这样可以为不同 target（主 App / 键盘扩展）指定不同 profile。
      - name: Archive (unsigned)
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          xcodebuild archive \
            -project "${{ env.PROJECT }}" \
            -scheme  "${{ env.SCHEME }}" \
            -configuration Release \
            -sdk iphoneos \
            -arch arm64 \
            -archivePath Hamster.xcarchive \
            CODE_SIGNING_ALLOWED=NO

      - name: Generate exportOptions.plist
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          cat > exportOptions.plist << 'EXPORT_EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>EXPORT_METHOD_PLACEHOLDER</string>
              <key>teamID</key>
              <string>TEAM_ID_PLACEHOLDER</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>signingCertificate</key>
              <string>Apple Distribution</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>BUNDLE_ID_MAIN_PLACEHOLDER</key>
                  <string>MAIN_PROFILE_PLACEHOLDER</string>
                  <key>BUNDLE_ID_KEYBOARD_PLACEHOLDER</key>
                  <string>KEYBOARD_PROFILE_PLACEHOLDER</string>
              </dict>
          </dict>
          </plist>
          EXPORT_EOF

          sed -i'' \
            -e "s|EXPORT_METHOD_PLACEHOLDER|${{ secrets.IOS_EXPORT_METHOD }}|g" \
            -e "s|TEAM_ID_PLACEHOLDER|${{ secrets.IOS_TEAM_ID }}|g" \
            -e "s|BUNDLE_ID_MAIN_PLACEHOLDER|${{ env.BUNDLE_ID_MAIN }}|g" \
            -e "s|MAIN_PROFILE_PLACEHOLDER|$MAIN_PROFILE_NAME|g" \
            -e "s|BUNDLE_ID_KEYBOARD_PLACEHOLDER|${{ env.BUNDLE_ID_KEYBOARD }}|g" \
            -e "s|KEYBOARD_PROFILE_PLACEHOLDER|$KEYBOARD_PROFILE_NAME|g" \
            exportOptions.plist

          echo "── exportOptions.plist ──"
          cat exportOptions.plist

      - name: Export IPA
        working-directory: ${{ env.HAMSTER_DIR }}
        run: |
          xcodebuild -exportArchive \
            -archivePath Hamster.xcarchive \
            -exportPath  Hamster-Release \
            -exportOptionsPlist exportOptions.plist
          echo "── 导出产物 ──"
          ls -lh Hamster-Release/

      # ── 打包 xcarchive（可在 Xcode Organizer 中重新导出或 resign）─────────────
      - name: Package xcarchive
        working-directory: ${{ env.HAMSTER_DIR }}
        run: tar -acf Hamster.xcarchive.tgz Hamster.xcarchive

      # ========== 准备发布产物 ==========
      - name: Prepare release artifacts
        run: |
          mkdir -p ./artifacts

          # Debug .app zip
          if [ "${{ needs.prepare.outputs.need_debug }}" = "true" ]; then
            if [ -f "${{ env.HAMSTER_DIR }}/Hamster-Debug-Simulator.zip" ]; then
              cp "${{ env.HAMSTER_DIR }}/Hamster-Debug-Simulator.zip" \
                "./artifacts/Hamster-${{ needs.prepare.outputs.app_version }}-Debug-Simulator.zip"
              echo "已创建: Hamster-${{ needs.prepare.outputs.app_version }}-Debug-Simulator.zip"
            fi
          fi

          # Release IPA
          find "${{ env.HAMSTER_DIR }}/Hamster-Release" -name "*.ipa" -type f 2>/dev/null | while read file; do
            cp "$file" "./artifacts/Hamster-${{ needs.prepare.outputs.app_version }}.ipa"
            echo "已复制: Hamster-${{ needs.prepare.outputs.app_version }}.ipa"
          done

          # xcarchive
          if [ -f "${{ env.HAMSTER_DIR }}/Hamster.xcarchive.tgz" ]; then
            cp "${{ env.HAMSTER_DIR }}/Hamster.xcarchive.tgz" \
              "./artifacts/Hamster-${{ needs.prepare.outputs.app_version }}.xcarchive.tgz"
            echo "已复制: Hamster-${{ needs.prepare.outputs.app_version }}.xcarchive.tgz"
          fi

          echo "=== 发布文件列表 ==="
          ls -lR ./artifacts/

      # ========== GitHub Release ==========
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.TOKEN }}
          tag_name: ${{ github.ref_name }}
          name: "${{ needs.prepare.outputs.app_display_name }} ${{ github.ref_name }}"
          body: |
            ## ${{ needs.prepare.outputs.app_display_name }} ${{ github.ref_name }}

            **版本号 / Version**: ${{ needs.prepare.outputs.app_version }}

            ### 📦 安装方式
            | 导出方式 | 安装途径 |
            |----------|---------|
            | `app-store` | TestFlight 内测 / App Store 正式发布 |
            | `ad-hoc`    | AltStore / Sideloadly 等工具侧载 |
            | `development` | Xcode 直接安装到已注册的开发设备 |

            ### 📁 文件说明
            | 文件 | 说明 |
            |------|------|
            | `Hamster-*.ipa` | iOS 安装包 |
            | `Hamster-*-Debug-Simulator.zip` | 模拟器 Debug 包（仅开启 debug 时生成） |
            | `Hamster-*.xcarchive.tgz` | Xcode 归档，可重新导出或 resign |

            ### 📝 变更内容
            详见 [Commits](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }})
          draft: false
          prerelease: false
          files: |
            ./artifacts/Hamster-*.ipa
            ./artifacts/Hamster-*-Debug-Simulator.zip
            ./artifacts/Hamster-*.xcarchive.tgz
          fail_on_unmatched_files: false
